def timeSync_decoder(a):
    timeSyncList = []

    for _ in a :

        timeSyncList.append(_)

    # year = str(timeSyncList[0])
    # month = str(timeSyncList[1])
    # day = str(timeSyncList[2])
    #
    # hour = str(timeSyncList[3])
    # minute = str(timeSyncList[4])
    # second = str(timeSyncList[5])
    # milisecond = timeSyncList

    # timeSync = f"{year}/{month}/{day};{hour}:{minute}:{second}:{milisecond}"
    return timeSyncList